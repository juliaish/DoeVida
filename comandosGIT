#colocando isso pra facilitar as atualizaçoes, importaçoes e exportaçoes porque eu nao sei os comandos de cor. 

#APAGAR AO FIM DO TRABALHO!

git init             # criar um repositório
git clone URL        # clona de um repositório remoto

git status           # ver alterações
git log --oneline    # histórico resumido

git add .            # adiciona tudo
git commit -m "msg"  # salva alterações

git branch nome      # nova branch
git checkout nome    # troca de branch(versao)
git checkout -b nome # cria e troca

git remote add origin URL  # conecta ao remoto
git push -u origin main    # sobe primeira vez
git push                   # envia alterações
git pull                   # baixa alterações

------------------------------------------------
ENTROU NO VSCODE FAZER:
git status        # 1 - verifica se você tem alterações locais
git branch        # 2 - confere em qual branch está (ex: main ou dev)
git fetch         # 3 - atualiza as referências do remoto
git pull          # 4 - puxa as mudanças do remoto para sua branch

AO SAIR:
git status          # 1 - vê se há arquivos modificados ou não rastreados
git add .           # 2 - adiciona todas as alterações à área de stage
git commit -m "Mensagem clara do que foi feito"   # 3 - cria um commit com descrição
git pull            # 4 - puxa mudanças do remoto para evitar conflitos
git push            # 5 - envia seu commit para o repositório remoto

#conta para teste: teste@gmail.com
#senha: Teste123!
